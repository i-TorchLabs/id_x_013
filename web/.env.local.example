# ====================================================================
# id_x_013 前端环境变量声明（Next.js 构建期注入）
#
# 使用方法：
#   cp .env.local.example .env.local
#   # 按需修改 .env.local 后执行 npm run build / npm run dev
#
# Next.js 在 next build / next dev 时自动加载 .env.local，
# 以 NEXT_PUBLIC_ 前缀的变量会被内联进客户端 bundle。
# 详见 src/utils/config.ts。
# ====================================================================

# API 基础地址：浏览器端用相对路径（留空），由 haproxy 同源转发到后端 service
NEXT_PUBLIC_BASE_URL=

# GraphQL 端点子路径（与后端路由前缀 /b/id_x_013/graphql 对齐）
NEXT_PUBLIC_SUB_VERSION=/b/id_x_013/graphql
